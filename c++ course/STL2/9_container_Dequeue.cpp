#include<bits/stdc++.h>
using namespace std;
 
/* global variable decleration here:- */

/* =====================   DEQUE   =================== */
/* 
  1.  dequeue is a doubly ended queue. it is similar to vector or it is better then
      vector because it provides you push operation in both the direction.
 */
 
int main()
{
   deque < int > dq;   //
 /* the function used in dequeue are 
 push_front()
 push_back()
 pop_front()
 pop_back()
 begin(), end(), rbegin(), rend()
 size()
 clear()
 empty()
 at() */

 
return 0;
}